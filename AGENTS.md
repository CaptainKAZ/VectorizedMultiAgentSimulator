# Layup 场景奖励系统详解

**生成时间**: 2026-03-06  
**场景**: vmas/layup (2v2 攻防对抗)  
**奖励缩放系数**: 
- 稠密奖励: `0.005 × dense_reward_factor` (默认 `dense_reward_factor = 0.1`, 实际缩放 `0.0005`)
- 终局奖励: `0.005` (无额外缩放)

---

## 目录

1. [终局奖励 (Terminal Rewards)](#1-终局奖励-terminal-rewards)
   - [进攻方获胜场景](#11-进攻方获胜场景)
   - [进攻方失败场景](#12-进攻方失败场景)
2. [稠密奖励 (Dense Rewards)](#2-稠密奖励-dense-rewards)
   - [通用奖励](#21-通用奖励-所有智能体)
   - [A1 (持球进攻者) 专属奖励](#22-a1-持球进攻者-专属奖励)
   - [A2 (无球掩护者) 专属奖励](#23-a2-无球掩护者-专属奖励)
   - [防守方专属奖励](#24-防守方专属奖励)
3. [奖励计算示例](#3-奖励计算示例)
4. [关键设计原则](#4-关键设计原则)
5. [超参数快速参考](#5-超参数快速参考)
6. [观察空间 (Observation Space)](#6-观察空间-observation-space)
   - [单智能体观察](#61-单智能体观察-agent-observation)
   - [全局状态](#62-全局状态-global-state)
   - [信息不对称设计](#64-信息不对称设计)
   - [⭐ 观测顺序详细报告](#68-观测顺序详细报告) ⭐ **重点章节**
   - [观察空间数值示例](#67-观察空间数值示例)
7. [⭐ Attention 模型配置与观测对应关系](#7-attention-模型配置与观测对应关系) ⭐ **新增章节**
   - [Actor配置详解](#72-actor-配置详解)
   - [Critic配置详解](#73-critic-配置详解)
   - [维度映射表](#74-维度映射表观察空间--attention-输入)
   - [配置验证清单](#77-配置验证清单)

---

## 1. 终局奖励 (Terminal Rewards)

### 1.1 进攻方获胜场景

#### 1.1.1 原因码 1: 投篮成功 (Shot Made)

**触发条件**: A1在投篮区静止10帧以上，且封盖因子 `< 0.5`

| 角色 | 奖励项 | 计算公式 | 原始最大值 | 缩放后最大值 | 说明 |
|------|--------|---------|-----------|-------------|------|
| **A1** | 基础得分 | `max_score × (1 - dist/R_spot) × (1 - block_factor)` | ~6000 | ~30.0 | 越靠近投篮区中心得分越高，被封盖程度越低得分越高 |
| | 时间奖励 | `k_time_bonus × (t_remaining/t_limit) × (1 - block_factor)` | ~6000 | ~30.0 | 剩余时间越多奖励越高 |
| | 空间奖励 | `k_spacing_bonus × avg_dist_to_defenders` | ~1000 | ~5.0 | 与防守方平均距离越远奖励越高 |
| | 速度静止奖励 | `k_shot_stillness_vel_bonus × exp(-speed)` | ~1000 | ~5.0 | 投篮瞬间速度越慢奖励越高 |
| | 动作静止奖励 | `k_shot_stillness_act_bonus × exp(-action_norm)` | 0 | 0.0 | 投篮瞬间动作指令越小奖励越高 |
| | 出手奖励 | `shoot_score` | 6000 | 30.0 | 成功出手的固定奖励 |
| **A1总计** | | | **~19000** | **~95.0** | 理论上限（完全静止、中心投篮、无封盖、最短时间） |
| **A2** | 基础得分 | `max_score × (1 - dist/R_spot) × (1 - block_factor)` | ~6000 | ~30.0 | 同A1 |
| | 掩护奖励 | `k_a2_screen_bonus × exp(-dist_to_ideal²/2σ²) × screen_gate` | ~3000 | ~15.0 | 成功掩护关键防守者 |
| | 空间奖励 | `k_spacing_bonus × avg_dist_to_defenders` | ~1000 | ~5.0 | 同A1 |
| | 时间奖励 | `k_time_bonus × (t_remaining/t_limit) × (1 - block_factor)` | ~6000 | ~30.0 | 同A1 |
| | 出手奖励 | `shoot_score` | 6000 | 30.0 | 同A1 |
| | 半场奖励 | `k_a2_shot_pos_bonus` (A2在防守半场) | 3000 | 15.0 | 投篮时A2已过中线 |
| **A2总计** | | | **~24000** | **~120.0** | 理论上限（完美掩护+过中线） |
| **防守方** | 封盖贡献 | `k_def_block_reward × block_contribution` | ~6000 | ~30.0 | 每个防守者根据封盖贡献获得奖励 |
| | 迫使远离 | `k_def_force_reward × (dist_a1_to_spot/R_spot)` | ~2000 | ~10.0 | 迫使A1远离投篮点 |
| | 站位奖励 | `k_def_pos_reward × exp(-dist_to_ideal²/2σ²) × pos_gate` | ~100 | ~0.5 | 占据理想防守位置 |
| | 区域控制 | `k_def_area_reward × exp(-dist_to_spot²/2σ²)` | ~150 | ~0.75 | 控制投篮区域 |
| | 时间拖延 | `k_def_delay_bonus × elapsed_time_ratio` | ~7500 | ~37.5 | 拖延时间越久奖励越高 |
| | 投篮惩罚 | `-k_def_shot_penalty` | -3000 | -15.0 | 基础惩罚（鼓励积极防守） |
| **防守方总计** | | | **~12750** | **~63.75** | 每个防守者 |

---

#### 1.1.2 原因码 2: 对手犯规 (Opponent Foul)

**触发条件**: 
- 高速碰撞（相对速度 > 0.5 m/s）或
- A1读条期间防守方触碰A1

| 角色 | 奖励项 | 计算公式 | 原始最大值 | 缩放后最大值 | 说明 |
|------|--------|---------|-----------|-------------|------|
| **进攻方** | 犯规奖励 | `R_foul + k_foul_vel_penalty × rel_velocity` | ~6000 | ~30.0 | 动态奖励，相对速度越快奖励越高 |
| | 队友奖励 | `foul_teammate_factor × 犯规奖励` | ~3600 | ~18.0 | 队友获得60%奖励 |
| **防守方** | 犯规惩罚 | `-(R_foul + k_foul_vel_penalty × rel_velocity)` | ~-7000 | ~-35.0 | 动态惩罚，相对速度越快惩罚越重 |

**特殊场景 - 读条期间犯规**:
- 防守方惩罚: `-R_foul` = -6000 (缩放后 -30.0)
- 进攻方奖励: `max_score` = 6000 (缩放后 30.0)

---

#### 1.1.3 原因码 3: 对手撞墙 (Opponent Wall Collision)

**触发条件**: 防守方持续撞墙 ≥ 20 帧

| 角色 | 奖励项 | 原始值 | 缩放后值 | 说明 |
|------|--------|--------|---------|------|
| **进攻方** | 胜利奖励 | +基础 | +基础 | 标准胜利奖励 |
| **防守方** | 撞墙惩罚 | -11000 | -55.0 | 极重惩罚，防止被推出界 |

---

#### 1.1.4 原因码 4: 对手越线 (Opponent Over Midline)

**触发条件**: 防守方越过中线 ≥ 20 帧

| 角色 | 奖励项 | 原始值 | 缩放后值 | 说明 |
|------|--------|--------|---------|------|
| **进攻方** | 胜利奖励 | +基础 | +基础 | 标准胜利奖励 |
| **防守方** | 越线惩罚 | -12000 | -60.0 | 最重惩罚，防止违反3秒规则 |

---

#### 1.1.5 原因码 5: 对手友军误伤 (Opponent Friendly Fire)

**触发条件**: 防守方高速碰撞队友

| 角色 | 奖励项 | 原始值 | 缩放后值 | 说明 |
|------|--------|--------|---------|------|
| **进攻方** | 胜利奖励 | +基础 | +基础 | 标准胜利奖励 |
| **防守方双方** | 误伤惩罚 | `-(R_foul + k_foul_vel_penalty × rel_velocity)` | ~-35.0 | 双方均受罚 |

---

### 1.2 进攻方失败场景

#### 1.2.1 原因码 11: 投篮被盖 (Shot Blocked)

**触发条件**: A1投篮但封盖因子 ≥ 0.5

| 角色 | 奖励项 | 计算公式 | 原始最大值 | 缩放后最大值 | 说明 |
|------|--------|---------|-----------|-------------|------|
| **防守方** | 封盖奖励 | `k_def_block_reward × block_contribution` | ~6000 | ~30.0 | 根据封盖贡献发放 |
| | 迫使远离 | `k_def_force_reward × (dist_a1_to_spot/R_spot)` | ~2000 | ~10.0 | 成功迫使A1远离 |
| | 其他奖励 | 同投篮成功场景 | ~5650 | ~28.25 | 站位、区域控制、时间拖延 |
| **防守方总计** | | | **~13650** | **~68.25** | 每个防守者 |
| **A1** | 基础得分 | `max_score × (1 - dist/R_spot) × (1 - block_factor)` | ~6000 | ~30.0 | 因高封盖因子而大幅降低 |
| | 其他奖励 | 同投篮成功场景 | ~13000 | ~65.0 | 但因高封盖因子而显著降低 |

---

#### 1.2.2 原因码 12: 进攻超时 (Attack Timeout)

**触发条件**: 时间耗尽（15秒）

| 角色 | 条件 | 计算公式 | 原始范围 | 缩放后范围 | 说明 |
|------|------|---------|---------|-----------|------|
| **A1** | 在投篮区 | `attacker_timeout_reward_in_spot - movement_penalty` | -3500 ~ +500 | -17.5 ~ +2.5 | 在圈内但超时，减去移动惩罚 |
| | 在投篮区外 | `-100 - k_timeout_dist_reward_factor × dist_to_spot` | -4000 ~ -100 | -20.0 ~ -0.5 | 离圈越远惩罚越重 |
| **A1总计** | | | **-4000 ~ +500** | **-20.0 ~ +2.5** | |
| **A2** | 队友系数 | `foul_teammate_factor × A1_reward` | -2400 ~ +300 | -12.0 ~ +1.5 | 获得60%的A1奖励 |
| | 偷懒惩罚 | `-k_a2_stalling_penalty × \|y\|` (y < 0) | -∞ | -∞ | 在己方半场被重罚 |
| **防守方** | 超时奖励 | `defender_timeout_reward` | +8500 | +42.5 | 固定奖励 |

---

#### 1.2.3 原因码 13: 己方犯规 (Team Foul)

**触发条件**: 进攻方高速碰撞对手

| 角色 | 奖励项 | 原始值 | 缩放后值 | 说明 |
|------|--------|--------|---------|------|
| **犯规进攻方** | 犯规惩罚 | `-(R_foul + k_foul_vel_penalty × rel_velocity)` | ~-35.0 | 动态惩罚 |
| **防守方** | 被犯规奖励 | `R_foul + k_foul_vel_penalty × rel_velocity` | ~35.0 | 动态奖励 |
| | 队友奖励 | `foul_teammate_factor × 犯规奖励` | ~21.0 | 另一防守者获得60% |

---

#### 1.2.4 原因码 14: 己方撞墙 (Team Wall Collision)

**触发条件**: 进攻方持续撞墙 ≥ 20 帧

| 角色 | 奖励项 | 原始值 | 缩放后值 | 说明 |
|------|--------|--------|---------|------|
| **撞墙进攻方** | 撞墙惩罚 | -11000 | -55.0 | 极重惩罚 |

---

#### 1.2.5 原因码 15: 己方友军误伤 (Team Friendly Fire)

**触发条件**: 进攻方高速碰撞队友

| 角色 | 奖励项 | 原始值 | 缩放后值 | 说明 |
|------|--------|--------|---------|------|
| **双方进攻方** | 误伤惩罚 | `-(R_foul + k_foul_vel_penalty × rel_velocity)` | ~-35.0 | 双方均受罚 |

---

## 2. 稠密奖励 (Dense Rewards)

**缩放因子**: `0.005 × dense_reward_factor` = `0.005 × 0.1` = **0.0005**

---

### 2.1 通用奖励 (所有智能体)

| 奖励项 | 计算公式 | 原始最大值 | 缩放后最大值 | 说明 |
|--------|---------|-----------|-------------|------|
| **出界惩罚** | `oob_penalty × margin × oob_depth × (speed + 1)` | -3000 × 0.05 × ∞ | -∞ | 越界越深惩罚越重，与速度相关 |
| **动作指令惩罚** | `-k_u_penalty_general × \|action\|` | -0.1 × 5 = -0.5 | -0.00025 | 惩罚大动作指令 |
| **动作超限惩罚** | `-k_action_access_max_penalty × excess / range` | -1.0 × (max_v / 0.05) | ~-0.1 | 动作超过95% v_max时额外惩罚 |
| **刹车使用惩罚** | `-k_brake_usage_penalty × is_braking` | -0.1 | -0.00005 | 惩罚刹车使用 |
| **矛盾动作惩罚** | `-k_conflicting_action_penalty × \|action\| × is_braking` | -1.0 × 5 = -5.0 | -0.0025 | 同时输出方向和刹车 |
| **超加速惩罚** | `-k_excess_acceleration_penalty × excess_accel` | -0.001 × large | -small | 请求加速度超过物理极限 |
| **动作平滑度惩罚** | `-k_action_jerk_penalty × \|action - prev_action\|` | -0.01 × 10 = -0.1 | -0.00005 | 惩罚动作突变 |
| **近距离惩罚** | `-k_proximity_penalty × penetration_depth` | -60 × large | -small | 与其他智能体过近 |
| **碰撞惩罚 (主动)** | `-k_coll_active × rel_velocity` | -5.0 × 5 = -25.0 | -0.0125 | 主动碰撞受重罚 |
| **碰撞惩罚 (被动)** | `-k_coll_passive × rel_velocity` | -0.1 × 5 = -0.5 | -0.00025 | 被动碰撞轻罚 |
| **推挤惩罚** | `-k_push_penalty × push_force` (进攻方) | -120 × 5 = -600 | -0.3 | 低速碰撞中推挤对手 |
| | `-k_def_push_penalty × push_force` (防守方) | -120 × 5 = -600 | -0.3 | 低速碰撞中推挤对手 |
| **造犯规奖励** | `k_stand_still_reward × speed_of_approach` | +10 × 5 = +50 | +0.025 | 站定诱使对手冲撞 |

**通用惩罚总计 (每步)**: **~-0.5 ~ -1.0** (正常游戏) 到 **~-50** (频繁碰撞)

---

### 2.2 A1 (持球进攻者) 专属奖励

| 奖励项 | 计算公式 | 原始最大值 | 缩放后最大值 | 说明 |
|--------|---------|-----------|-------------|------|
| **高斯吸引奖励** | `gaussian_scale × exp(-dist²/2σ²)` | 600 | 0.3 | 吸引A1进入投篮区 |
| **路径速度奖励** | `k × velocity_projection_to_spot` | 6000 (全程) | 3.0 (全程) | 奖励朝投篮点移动 |
| **圈内存在奖励** | `k_a1_in_spot_reward × (1.5 - dist/R_spot)` | 3.0 × 1.5 = 4.5 | 0.00225 | 在圈内持续获得奖励 |
| **准备投篮奖励** | `k_a1_ready_to_shoot_reward` | 200 | 0.1 | 处于准备投篮状态 |
| **速度静止奖励** | `k_a1_velocity_stillness_reward × exp(-speed²/2σ²)` (圈内) | 20 | 0.01 | 圈内速度越慢奖励越高 |
| **动作静止奖励** | `k_a1_action_stillness_reward × exp(-action²/2σ²)` (圈内) | 10 | 0.005 | 圈内动作越小奖励越高 |
| **刹车奖励** | `k_a1_brake_in_spot_reward × is_braking` (圈内) | 20 | 0.01 | 圈内刹车奖励 |
| **被封盖惩罚** | `k_a1_blocked_penalty × block_factor` | -90 × 1 = -90 | -0.045 | 投篮路线被封锁 |
| **犹豫惩罚** | `-k_hesitation_penalty × hesitation_factor` (圈外) | -90 | -0.045 | 圈外移动过慢 |
| **分离奖励** | `k_a1_separation_reward × speed_away_from_defender` (被封时) | 60 × 5 = 300 | 0.15 | 被封时向远离防守者方向移动 |
| **横向机动奖励** | `k_a1_tangential_reward × tangential_speed × pressure_gate` | 120 × 5 = 600 | 0.3 | 受压迫时横向移动 |
| **放弃投篮惩罚** | `-0.5 × k_a1_ready_to_shoot_reward` | -100 | -0.05 | 开始读条后放弃 |

**A1 稠密奖励总计 (每步)**: **~-1.0 ~ +3.5** (圈内静止最优) 到 **~-2.0** (圈外犹豫/被封)

---

### 2.3 A2 (无球掩护者) 专属奖励

**重要**: A2 的正向奖励仅在 `y >= 0` (越过中线) 时生效，否则奖励被限制为非正值

| 奖励项 | 计算公式 | 原始最大值 | 缩放后最大值 | 说明 |
|--------|---------|-----------|-------------|------|
| **掩护位置奖励** | `k_ideal_screen_pos × exp(-dist_to_ideal²/2σ²) × pos_gate × spacing_gate` | 200 | 0.1 | 移动到理想掩护位置 |
| **干扰奖励** | `k_a2_interference_reward × exp(-dist_to_def²/2σ²)` | 400 | 0.2 | 靠近并干扰防守者 |
| **排斥奖励** | `k_repulsion_reward × defender_retreat_speed × proximity_gate` | 800 × 5 = 4000 | 2.0 | 迫使防守者远离A1 |
| **投篮路线阻挡惩罚** | `-k_a2_shot_line_penalty × line_block_factor × proximity_factor` | -90 | -0.045 | 挡住A1投篮视线 |

**A2 稠密奖励总计 (每步)**: **~-0.5 ~ +2.3** (过线且完美掩护) 到 **~-0.5** (不过线或阻挡路线)

---

### 2.4 防守方专属奖励

| 奖励项 | 计算公式 | 原始最大值 | 缩放后最大值 | 说明 |
|--------|---------|-----------|-------------|------|
| **越过中线惩罚** | `-k_overextend_penalty × max(0, -y)` | -240 × large | -large | 越过中线受罚 |
| **站位奖励** | `k_positioning × exp(-dist_to_ideal²/2σ²) × behind_gate × in_def_half` | 140 | 0.07 | 占据A1和篮筐之间 |
| **压迫奖励** | `k_def_pressure_reward × (1 - dist/range)² × in_def_half × gate` | 30 | 0.015 | 靠近A1施加压力 |
| **A1突破惩罚** | `-k_def_a1_penetration_penalty × a1_depth²` | -20 × 7.5² = -1125 | -0.5625 | A1越深入惩罚越重 |
| **区域控制奖励** | `k_spot_control_reward × (-radial_vel) × gate` | 100 × 5 = 500 | 0.25 | 阻止A1向篮筐移动 |
| **高斯区域奖励** | `k_def_gaussian_spot × exp(-dist_to_spot²/2σ²) × in_def_half` | 30 | 0.015 | 占据投篮区中心 |
| **读条期间稳定奖励** | `k_def_charging_stability × position_quality × is_charging × is_slow × in_def_half` | 3000 | 1.5 | A1读条时防守方站位稳定且速度慢 |

**防守方稠密奖励总计 (每步)**: **~-1.5 ~ +2.0** (完美站位) 到 **~-50** (严重越线/被突破)

---

### 2.5 时间压力奖励

**触发条件**: 时间 > 8 秒（宽限期后）

| 角色 | 计算公式 | 原始范围 | 缩放后范围 | 说明 |
|------|---------|---------|-----------|------|
| **进攻方** (A1不在圈内) | `-k_attacker_time_penalty × (elapsed - 8)²` | -0.5 × 49 = -24.5 | -0.01225 | 时间紧迫但未进入投篮区 |
| **防守方** | `+k_defender_time_bonus × (elapsed - 8)²` | +0.1 × 49 = +4.9 | +0.00245 | 时间流逝对防守有利 |

---

## 3. 奖励计算示例

### 3.1 完美进攻 (快速投篮成功)

**场景**: A1快速进入投篮区中心，A2完美掩护，防守方站位失败

| 阶段 | A1奖励 | A2奖励 | D1奖励 | D2奖励 |
|------|--------|--------|--------|--------|
| 路径移动 (100步) | +300 (速度奖励) | +200 (掩护奖励) | -50 (被突破) | -50 (被突破) |
| 圈内准备 (10步) | +20 (静止奖励) | +20 (干扰奖励) | +10 (压迫) | +10 (压迫) |
| **终局奖励** | **+95** | **+120** | **+30** | **+30** |
| **总计** | **~415** | **~340** | **~-10** | **~-10** |

---

### 3.2 防守成功 (投篮被盖)

**场景**: A1勉强投篮，防守方完美封盖

| 阶段 | A1奖励 | A2奖励 | D1奖励 | D2奖励 |
|------|--------|--------|--------|--------|
| 路径移动 (200步) | +200 | +100 | +100 (站位) | +100 (站位) |
| 尝试投篮 | +20 | +20 | +30 (压迫) | +30 (压迫) |
| **终局奖励** | **+30** (高封盖因子) | **+30** | **+68** | **+68** |
| **总计** | **~250** | **~150** | **~198** | **~198** |

---

### 3.3 超时失败

**场景**: A1未能进入投篮区，时间耗尽

| 阶段 | A1奖励 | A2奖励 | D1奖励 | D2奖励 |
|------|--------|--------|--------|--------|
| 移动尝试 (150步) | +100 | +50 | +150 (站位) | +150 (站位) |
| 时间压力 (70步) | -15 (超时惩罚) | -9 | +3.5 | +3.5 |
| **终局奖励** | **-10** | **-6** | **+42.5** | **+42.5** |
| **总计** | **~75** | **~35** | **~196** | **~196** |

---

## 4. 关键设计原则

### 4.1 奖励稀疏性

- **终局奖励占比**: ~70-90% 的总奖励来自终局
- **稠密奖励作用**: 主要用于行为塑造和引导，避免局部最优

### 4.2 动态策略切换

- **A1行为**: 根据封盖因子在"静止投篮"和"移动摆脱"间切换
  - `未被封盖 (block_factor < 0.5)`: 奖励静止准备
  - `被封盖 (block_factor ≥ 0.5)`: 奖励远离防守者

### 4.3 防守方防作弊机制

- **读条期间稳定奖励**: 防止防守方在A1准备投篮时冲刺盖帽
- **速度门控**: 投篮判定时，防守方速度越快，封盖贡献越低

### 4.4 A2过线约束

- **正向奖励门控**: A2的所有正向奖励仅在 `y >= 0` 时生效
- **设计意图**: 强制A2积极过线参与进攻，避免消极等待

### 4.5 时间压力平衡

- **宽限期**: 前8秒无时间惩罚，给进攻方充分时间组织
- **压力递增**: 8秒后，惩罚/奖励与时间的平方成正比

---

## 5. 超参数快速参考

| 参数名 | 默认值 | 说明 |
|--------|--------|------|
| `dense_reward_factor` | 0.1 | 稠密奖励整体缩放 |
| `max_score` | 6000 | 投篮基础得分 |
| `shoot_score` | 6000 | 出手固定奖励 |
| `R_foul` | 6000 | 犯规基础奖励/惩罚 |
| `R_wall_collision_penalty` | -11000 | 撞墙惩罚 |
| `R_midline_foul` | 12000 | 越线犯规惩罚 |
| `defender_timeout_reward` | 8500 | 防守方超时奖励 |
| `shot_still_frames` | 10 | 投篮准备帧数 |
| `v_shot_threshold` | 0.2 | 投篮速度阈值 (训练时curriculum learning) |
| `max_time_over_midline` | 20 | 防守方允许越线帧数 |

---

## 6. 观察空间 (Observation Space)

### 6.1 单智能体观察 (Agent Observation)

每个智能体接收一个 **23维** 的归一化观察向量，包含相对位置、相对速度和游戏状态信息。

#### 6.1.1 观察向量结构

| 维度索引 | 内容 | 归一化方式 | 数值范围 | 说明 |
|---------|------|-----------|---------|------|
| **[0:2]** | 自身位置 (x, y) | `pos / [W/2, L/2]` | [-1, 1] | 绝对位置，场地中心为原点 |
| **[2:4]** | 自身速度 (vx, vy) | `vel / v_max` | [-1, 1] | 当前速度 |
| **[4:5]** | A1是否在投篮区 | 0 或 1 | {0, 1} | 所有智能体可见 |
| **[5:6]** | A1读条进度 | `counter / shot_still_frames` | [0, 1] | 0=未开始，1=准备完成 |
| **[6:7]** | 剩余时间比例 | `t_remaining / t_limit` | [0, 1] | 1=刚开局，0=时间耗尽 |
| **[7:9]** | 队友相对位置 | `(teammate_pos - self_pos) / [W, L]` | [-1, 1] | 相对于自身的位置 |
| **[9:11]** | 队友相对速度 | `(teammate_vel - self_vel) / (2 × v_max)` | [-1, 1] | 相对于自身的速度 |
| **[11:13]** | 对手1相对位置 | `(opp1_pos - self_pos) / [W, L]` | [-1, 1] | 相对于自身的位置 |
| **[13:15]** | 对手1相对速度 | `(opp1_vel - self_vel) / (2 × v_max)` | [-1, 1] | 相对于自身的速度 |
| **[15:17]** | 对手2相对位置 | `(opp2_pos - self_pos) / [W, L]` | [-1, 1] | 相对于自身的位置 |
| **[17:19]** | 对手2相对速度 | `(opp2_vel - self_vel) / (2 × v_max)` | [-1, 1] | 相对于自身的速度 |
| **[19:21]** | 投篮点相对位置 | `(spot_pos - self_pos) / [W, L]` | [-1, 1] | **进攻方可见，防守方为0** |
| **[21:23]** | 篮筐相对位置 | `(basket_pos - self_pos) / [W, L]` | [-1, 1] | 所有智能体可见 |

**总计**: **23维**

---

#### 6.1.2 不同角色的观察差异

| 信息项 | A1 (持球人) | A2 (掩护者) | D1, D2 (防守方) | 说明 |
|--------|------------|------------|----------------|------|
| 自身状态 | ✅ | ✅ | ✅ | 完全可见 |
| A1在圈内 | ✅ | ✅ | ✅ | 所有智能体共享 |
| A1读条进度 | ✅ | ✅ | ✅ | 所有智能体共享 |
| 队友状态 | A2 | A1 | 另一防守方 | 相对位置+相对速度 |
| 对手状态 | D1, D2 | D1, D2 | A1, A2 | 相对位置+相对速度 |
| 投篮点位置 | ✅ **可见** | ✅ **可见** | ❌ **不可见 (全0)** | **关键信息不对称** |
| 篮筐位置 | ✅ | ✅ | ✅ | 所有智能体可见 |

---

### 6.2 全局状态 (Global State)

全局状态用于 **Centralized Critic**（集中式价值函数），包含所有智能体的完整状态信息，维度为 **23维**。

#### 6.2.1 全局状态结构

| 维度索引 | 内容 | 归一化方式 | 数值范围 | 说明 |
|---------|------|-----------|---------|------|
| **[0:2]** | A1位置 (x, y) | `pos / [W/2, L/2]` | [-1, 1] | 持球人位置 |
| **[2:4]** | A1速度 (vx, vy) | `vel / v_max` | [-1, 1] | 持球人速度 |
| **[4:5]** | A1是否在投篮区 | 0 或 1 | {0, 1} | 在圈内=1 |
| **[5:6]** | A1读条进度 | `counter / shot_still_frames` | [0, 1] | 准备投篮进度 |
| **[6:8]** | A2位置 (x, y) | `pos / [W/2, L/2]` | [-1, 1] | 掩护者位置 |
| **[8:10]** | A2速度 (vx, vy) | `vel / v_max` | [-1, 1] | 掩护者速度 |
| **[10:12]** | D1位置 (x, y) | `pos / [W/2, L/2]` | [-1, 1] | 防守方1位置 |
| **[12:14]** | D1速度 (vx, vy) | `vel / v_max` | [-1, 1] | 防守方1速度 |
| **[14:16]** | D2位置 (x, y) | `pos / [W/2, L/2]` | [-1, 1] | 防守方2位置 |
| **[16:18]** | D2速度 (vx, vy) | `vel / v_max` | [-1, 1] | 防守方2速度 |
| **[18:20]** | 投篮点位置 (x, y) | `pos / [W/2, L/2]` | [-1, 1] | 投篮区中心位置 |
| **[20:22]** | 篮筐位置 (x, y) | `pos / [W/2, L/2]` | [-1, 1] | 篮筐位置 |
| **[22:23]** | 剩余时间比例 | `t_remaining / t_limit` | [0, 1] | 时间归一化 |

**总计**: **23维**

---

#### 6.2.2 全局状态 vs 单智能体观察对比

| 特性 | 全局状态 | 单智能体观察 |
|------|---------|-------------|
| **维度** | 23维 | 23维 |
| **位置表示** | 绝对位置（归一化） | 相对位置（相对于自身） |
| **速度表示** | 绝对速度（归一化） | 相对速度（相对于自身） |
| **信息完整性** | 所有智能体完整状态 | 自身视角的相对状态 |
| **投篮点可见性** | ✅ 始终可见 | 进攻方可见，防守方为0 |
| **用途** | Centralized Critic | Actor决策输入 |
| **参数共享** | 所有智能体共享同一个Critic | 每个智能体独立Actor |

---

### 6.3 归一化参数详解

#### 6.3.1 位置归一化

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `W` (场地宽度) | 8.0 m | X轴半宽 = 4.0 m |
| `L` (场地长度) | 15.0 m | Y轴半长 = 7.5 m |
| **位置归一化因子** | `[W/2, L/2]` = `[4.0, 7.5]` | 绝对位置除以此因子 |
| **相对位置归一化因子** | `[W, L]` = `[8.0, 15.0]` | 相对位置除以此因子 |

**示例**:
- 自身位置 `[2.0, 3.0]` → 归一化 `[0.5, 0.4]`
- 队友相对位置 `[4.0, -2.0]` → 归一化 `[0.5, -0.133]`

---

#### 6.3.2 速度归一化

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `v_max` (最大速度) | 5.0 m/s | 智能体速度上限 |
| **绝对速度归一化因子** | `v_max` = 5.0 | 绝对速度除以5.0 |
| **相对速度归一化因子** | `2 × v_max` = 10.0 | 相对速度除以10.0 |

**示例**:
- 自身速度 `[3.0, -2.0]` → 归一化 `[0.6, -0.4]`
- 队友相对速度 `[6.0, -4.0]` → 归一化 `[0.6, -0.4]`

---

#### 6.3.3 时间归一化

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `t_limit` (最大时间) | 15.0 秒 | 每回合时长上限 |
| **时间归一化因子** | `t_limit` = 15.0 | 剩余时间除以15.0 |

**示例**:
- 剩余时间 7.5秒 → 归一化 `0.5`
- 剩余时间 15.0秒 → 归一化 `1.0`

---

### 6.4 信息不对称设计

#### 6.4.1 投篮点位置（Spot）

**设计**: 防守方观察中的投篮点位置为 **全0向量**

| 角色 | 投篮点相对位置 [19:21] | 设计意图 |
|------|---------------------|---------|
| **进攻方 (A1, A2)** | ✅ 真实相对位置 | 知道目标位置，可以规划路径 |
| **防守方 (D1, D2)** | ❌ `[0.0, 0.0]` | 不知道目标位置，需要通过A1行为推断 |

**游戏策略影响**:
- 进攻方：明确目标，直接导航到投篮点
- 防守方：需要通过观察A1的移动方向、速度、位置来推断投篮点位置
- 防守策略：重点防守A1可能进入的区域，通过站位和压迫限制A1活动空间

---

#### 6.4.2 A1状态共享

**设计**: A1的"是否在圈内"和"读条进度"对所有智能体可见

| 信息项 | 所有智能体可见 | 设计意图 |
|--------|--------------|---------|
| **A1在投篮区** | ✅ | 防守方知道何时进入关键防守期 |
| **A1读条进度** | ✅ | 所有智能体知道投篮准备阶段，防守方可以判断封盖时机 |

**游戏策略影响**:
- 进攻方A2：知道何时提供掩护、何时干扰防守
- 防守方：知道何时施加最大压力、何时准备封盖
- 透明机制：避免"突然死亡"的不公平感

---

### 6.5 观察空间代码实现

#### 6.5.1 单智能体观察生成逻辑

```python
def observation(self, agent: Agent):
    # 1. 确定角色和索引
    agent_idx = self.world.agents.index(agent)
    is_attacker = agent_idx < self.n_attackers
    
    # 2. 根据角色确定队友和对手索引
    if is_attacker:
        teammate_idx = 1 - agent_idx  # A1的队友是A2，A2的队友是A1
        opp1_idx, opp2_idx = 2, 3      # 对手是D1, D2
    else:
        teammate_idx = 5 - agent_idx   # D1的队友是D2，D2的队友是D1
        opp1_idx, opp2_idx = 0, 1      # 对手是A1, A2
    
    # 3. 计算归一化的相对位置和速度
    teammate_obs = [相对位置, 相对速度]  # 4维
    opp1_obs = [相对位置, 相对速度]      # 4维
    opp2_obs = [相对位置, 相对速度]      # 4维
    
    # 4. 投篮点位置（防守方看不到）
    spot_obs = spot_rel_pos if is_attacker else [0.0, 0.0]
    
    # 5. 拼接完整观察向量
    obs = [
        self_obs,           # [4] 自身位置+速度
        is_in_spot_a1,      # [1] A1是否在圈内
        a1_shoot_process,   # [1] A1读条进度
        time_obs,           # [1] 剩余时间
        teammate_obs,       # [4] 队友相对状态
        opp1_obs,           # [4] 对手1相对状态
        opp2_obs,           # [4] 对手2相对状态
        spot_obs,           # [2] 投篮点相对位置
        basket_rel_pos,     # [2] 篮筐相对位置
    ]  # 总计 23维
```

---

#### 6.5.2 全局状态生成逻辑

```python
def get_global_state(self):
    # 1. 归一化所有智能体状态
    norm_all_pos = all_pos / [W/2, L/2]  # 绝对位置归一化
    norm_all_vel = all_vel / v_max       # 绝对速度归一化
    
    # 2. A1状态（包含额外信息）
    a1_state = [
        norm_all_pos[0],    # [2] 位置
        norm_all_vel[0],    # [2] 速度
        is_in_spot_a1,      # [1] 是否在圈内
        shoot_progress,     # [1] 读条进度
    ]  # 总计 6维
    
    # 3. 其他智能体状态
    a2_state = [位置, 速度]  # 4维
    d1_state = [位置, 速度]  # 4维
    d2_state = [位置, 速度]  # 4维
    
    # 4. 环境信息
    spot_pos = 投篮点位置      # 2维
    basket_pos = 篮筐位置      # 2维
    time_obs = 剩余时间        # 1维
    
    # 5. 拼接全局状态
    global_state = [
        a1_state,    # 6维
        a2_state,    # 4维
        d1_state,    # 4维
        d2_state,    # 4维
        spot_pos,    # 2维
        basket_pos,  # 2维
        time_obs,    # 1维
    ]  # 总计 23维
```

---

### 6.6 观察空间设计原则

#### 6.6.1 归一化的必要性

| 原因 | 说明 |
|------|------|
| **数值稳定性** | 避免不同维度数值差异过大，影响神经网络训练 |
| **梯度传播** | 归一化后的数值范围在[-1, 1]，梯度传播更稳定 |
| **泛化能力** | 归一化后模型更容易学习到通用规律 |
| **参数共享** | 归一化后的观察便于不同智能体共享网络参数 |

---

#### 6.6.2 相对坐标 vs 绝对坐标

| 特性 | 单智能体观察（相对坐标） | 全局状态（绝对坐标） |
|------|---------------------|-------------------|
| **优点** | 不受智能体初始位置影响，泛化性好 | 完整描述全局态势 |
| **缺点** | 缺乏全局位置信息 | 需要额外处理位置变换 |
| **适用场景** | Actor决策（分散式执行） | Critic评估（集中式训练） |
| **实现** | 每步计算相对位置 | 直接使用绝对位置 |

---

#### 6.6.3 信息不对称的价值

| 设计 | 游戏意义 |
|------|---------|
| **投篮点对防守方不可见** | 模拟真实篮球中防守方不知道进攻方具体战术目标 |
| **A1状态对所有方可见** | 持球人的位置和状态是公共信息（球在场上的位置是可见的） |
| **相对速度可见** | 智能体可以感知对手的运动趋势 |

---

### 6.7 观察空间数值示例

#### 6.7.1 A1（持球人）观察示例

**场景**: A1在 `[1.5, 3.0]` 位置，速度 `[0.5, -0.3]`，在投篮区内，读条进度 0.5

```
观察向量 (23维):
[0]  0.375   # 自身位置 x (1.5/4.0)
[1]  0.400   # 自身位置 y (3.0/7.5)
[2]  0.100   # 自身速度 vx (0.5/5.0)
[3] -0.060   # 自身速度 vy (-0.3/5.0)
[4]  1.000   # A1在投篮区
[5]  0.500   # A1读条进度
[6]  0.800   # 剩余时间比例 (12秒/15秒)
[7] -0.125   # 队友(A2)相对位置 x
[8]  0.200   # 队友(A2)相对位置 y
[9]  0.050   # 队友(A2)相对速度 vx
[10] 0.100   # 队友(A2)相对速度 vy
[11] 0.250   # 对手1(D1)相对位置 x
[12] 0.150   # 对手1(D1)相对位置 y
[13] 0.080   # 对手1(D1)相对速度 vx
[14] 0.040   # 对手1(D1)相对速度 vy
[15] -0.300  # 对手2(D2)相对位置 x
[16] 0.100   # 对手2(D2)相对位置 y
[17] 0.020   # 对手2(D2)相对速度 vx
[18] -0.060  # 对手2(D2)相对速度 vy
[19] 0.050   # 投篮点相对位置 x (进攻方可见)
[20] 0.100   # 投篮点相对位置 y
[21] 0.300   # 篮筐相对位置 x
[22] 0.600   # 篮筐相对位置 y
```

---

#### 6.7.2 D1（防守方）观察示例

**场景**: D1在 `[2.0, 5.0]` 位置，速度 `[-0.2, 0.1]`

```
观察向量 (23维):
[0]  0.500   # 自身位置 x (2.0/4.0)
[1]  0.667   # 自身位置 y (5.0/7.5)
[2] -0.040   # 自身速度 vx (-0.2/5.0)
[3]  0.020   # 自身速度 vy (0.1/5.0)
[4]  1.000   # A1在投篮区 (所有方可见)
[5]  0.500   # A1读条进度 (所有方可见)
[6]  0.800   # 剩余时间比例
[7]  0.100   # 队友(D2)相对位置 x
[8] -0.050   # 队友(D2)相对位置 y
[9]  0.030   # 队友(D2)相对速度 vx
[10] 0.020   # 队友(D2)相对速度 vy
[11] -0.125  # 对手1(A1)相对位置 x
[12] -0.267  # 对手1(A1)相对位置 y
[13] 0.100   # 对手1(A1)相对速度 vx
[14] -0.080  # 对手1(A1)相对速度 vy
[15] -0.200  # 对手2(A2)相对位置 x
[16] -0.300  # 对手2(A2)相对位置 y
[17] 0.050   # 对手2(A2)相对速度 vx
[18] 0.100   # 对手2(A2)相对速度 vy
[19] 0.000   # 投篮点相对位置 x (防守方不可见，全0)
[20] 0.000   # 投篮点相对位置 y
[21] 0.250   # 篮筐相对位置 x
[22] 0.500   # 篮筐相对位置 y
```

---

### 6.8 观测顺序详细报告

本节重点说明**全局状态观测顺序**与**单智能体观测顺序**的关键差异，这是理解MARL架构的核心。

---

#### 6.8.1 全局状态观测顺序（固定顺序）

**关键特性**: 全局状态使用**固定的智能体顺序**，不随观察者身份变化。

```
全局状态向量顺序 (23维):

[0:2]   A1 位置 (x, y)          ← 持球进攻者
[2:4]   A1 速度 (vx, vy)        
[4:5]   A1 在圈内标志           
[5:6]   A1 读条进度             

[6:8]   A2 位置 (x, y)          ← 掩护者
[8:10]  A2 速度 (vx, vy)        

[10:12] D1 位置 (x, y)          ← 防守方1
[12:14] D1 速度 (vx, vy)        

[14:16] D2 位置 (x, y)          ← 防守方2
[16:18] D2 速度 (vx, vy)        

[18:20] 投篮点位置 (x, y)       
[20:22] 篮筐位置 (x, y)         
[22:23] 剩余时间比例            
```

**顺序规则**:
1. **智能体顺序固定**: A1 → A2 → D1 → D2
2. **信息排列一致**: 位置 → 速度 → (特殊标志)
3. **使用场景**: Centralized Critic（所有智能体共享同一个Critic）

---

#### 6.8.2 单智能体观测顺序（相对顺序）

**关键特性**: 单智能体观测使用**相对于自身的顺序**，观察者和被观察者的身份决定观测内容。

---

##### A1（持球人）的观测顺序

```
A1 观察向量 (23维):

[0:2]   自身位置 (A1)           ← 观察者自己
[2:4]   自身速度 (A1)           
[4:5]   A1 在圈内                ← 自身状态标志
[5:6]   A1 读条进度              
[6:7]   剩余时间                 

[7:9]   队友位置 (A2)           ← 队友是A2
[9:11]  队友速度 (A2)           

[11:13] 对手1位置 (D1)          ← 对手是D1, D2
[13:15] 对手1速度 (D1)          
[15:17] 对手2位置 (D2)          
[17:19] 对手2速度 (D2)          

[19:21] 投篮点相对位置           ← 进攻方可见
[21:23] 篮筐相对位置            
```

**观测身份映射**:
- 观察者: **A1**
- 队友: **A2**
- 对手: **D1, D2** (按world.agents顺序)

---

##### A2（掩护者）的观测顺序

```
A2 观察向量 (23维):

[0:2]   自身位置 (A2)           ← 观察者自己
[2:4]   自身速度 (A2)           
[4:5]   A1 在圈内                ← A1状态标志
[5:6]   A1 读条进度              
[6:7]   剩余时间                 

[7:9]   队友位置 (A1)           ← 队友是A1
[9:11]  队友速度 (A1)           

[11:13] 对手1位置 (D1)          ← 对手是D1, D2
[13:15] 对手1速度 (D1)          
[15:17] 对手2位置 (D2)          
[17:19] 对手2速度 (D2)          

[19:21] 投篮点相对位置           ← 进攻方可见
[21:23] 篮筐相对位置            
```

**观测身份映射**:
- 观察者: **A2**
- 队友: **A1**
- 对手: **D1, D2** (按world.agents顺序)

---

##### D1（防守方1）的观测顺序

```
D1 观察向量 (23维):

[0:2]   自身位置 (D1)           ← 观察者自己
[2:4]   自身速度 (D1)           
[4:5]   A1 在圈内                ← A1状态标志
[5:6]   A1 读条进度              
[6:7]   剩余时间                 

[7:9]   队友位置 (D2)           ← 队友是D2
[9:11]  队友速度 (D2)           

[11:13] 对手1位置 (A1)          ← 对手是A1, A2
[13:15] 对手1速度 (A1)          
[15:17] 对手2位置 (A2)          
[17:19] 对手2速度 (A2)          

[19:21] 投篮点相对位置           ← 防守方不可见 (全0)
[21:23] 篮筐相对位置            
```

**观测身份映射**:
- 观察者: **D1**
- 队友: **D2**
- 对手: **A1, A2** (按world.agents顺序)

---

##### D2（防守方2）的观测顺序

```
D2 观察向量 (23维):

[0:2]   自身位置 (D2)           ← 观察者自己
[2:4]   自身速度 (D2)           
[4:5]   A1 在圈内                ← A1状态标志
[5:6]   A1 读条进度              
[6:7]   剩余时间                 

[7:9]   队友位置 (D1)           ← 队友是D1
[9:11]  队友速度 (D1)           

[11:13] 对手1位置 (A1)          ← 对手是A1, A2
[13:15] 对手1速度 (A1)          
[15:17] 对手2位置 (A2)          
[17:19] 对手2速度 (A2)          

[19:21] 投篮点相对位置           ← 防守方不可见 (全0)
[21:23] 篮筐相对位置            
```

**观测身份映射**:
- 观察者: **D2**
- 队友: **D1**
- 对手: **A1, A2** (按world.agents顺序)

---

#### 6.8.3 观测顺序对比矩阵

| 观察者 | 自身 | 队友 | 对手1 | 对手2 | 投篮点 |
|--------|------|------|-------|-------|--------|
| **A1** | A1 | A2 | D1 | D2 | ✅ 可见 |
| **A2** | A2 | A1 | D1 | D2 | ✅ 可见 |
| **D1** | D1 | D2 | A1 | A2 | ❌ 全0 |
| **D2** | D2 | D1 | A1 | A2 | ❌ 全0 |

**关键发现**:
1. **队友身份动态变化**: A1的队友是A2，A2的队友是A1，D1的队友是D2，D2的队友是D1
2. **对手顺序固定**: 对手按world.agents顺序排列（进攻方先A1后A2，防守方先D1后D2）
3. **A1状态标志共享**: 所有智能体都能看到A1的"在圈内"和"读条进度"状态

---

#### 6.8.4 为什么需要两种不同的观测顺序？

| 维度 | 全局状态（固定顺序） | 单智能体观测（相对顺序） |
|------|-------------------|---------------------|
| **目的** | 集中式价值评估 | 分散式决策 |
| **使用者** | Critic网络 | Actor网络 |
| **参数共享** | 所有智能体共享一个Critic | 每个智能体独立Actor |
| **泛化能力** | 固定顺序便于学习全局态势 | 相对顺序不受初始位置影响 |
| **训练阶段** | 训练时可用（集中式训练） | 训练和执行都可用（分散式执行） |
| **信息完整性** | 包含所有智能体完整状态 | 每个智能体只看自己相关的信息 |

---

#### 6.8.5 代码实现：观测身份映射

```python
def observation(self, agent: Agent):
    agent_idx = self.world.agents.index(agent)
    is_attacker = agent_idx < self.n_attackers
    
    # 根据角色确定队友和对手索引
    if is_attacker:
        # 进攻方：队友是另一个进攻方，对手是防守方
        teammate_idx = 1 - agent_idx  # A1→A2(1), A2→A1(0)
        opp1_idx, opp2_idx = self.n_attackers, self.n_attackers + 1  # D1(2), D2(3)
    else:
        # 防守方：队友是另一个防守方，对手是进攻方
        teammate_idx = 5 - agent_idx  # D1(2)→D2(3), D2(3)→D1(2)
        opp1_idx, opp2_idx = 0, 1  # A1(0), A2(1)
    
    # 计算相对位置和相对速度
    teammate_obs = 计算相对状态(teammate_idx)
    opp1_obs = 计算相对状态(opp1_idx)
    opp2_obs = 计算相对状态(opp2_idx)
    
    # 投篮点位置（仅进攻方可见）
    spot_obs = spot_rel_pos if is_attacker else torch.zeros_like(spot_rel_pos)
    
    return [self_obs, a1_state, time_obs, teammate_obs, opp1_obs, opp2_obs, spot_obs, basket_obs]
```

**关键逻辑**:
- `1 - agent_idx`: A1(0) → A2(1), A2(1) → A1(0)
- `5 - agent_idx`: D1(2) → D2(3), D2(3) → D1(2)
- 对手索引：进攻方看到[D1, D2]，防守方看到[A1, A2]

---

#### 6.8.6 实战意义

**进攻方策略（A1/A2）**:
- 观察到**真实的投篮点位置**，可以规划最优路径
- 观察到**队友的相对位置**，可以配合掩护战术
- 观察到**防守方的相对位置**，可以判断被封盖程度

**防守方策略（D1/D2）**:
- **看不到投篮点位置**（全0），必须通过A1的移动轨迹推断目标
- 观察到**队友的相对位置**，可以配合防守站位
- 观察到**进攻方的相对位置**，特别是A1的移动方向和速度

**信息不对称的价值**:
- 进攻方：需要明确的战术执行（导航到投篮点）
- 防守方：需要战术推断和适应性防守（观察→推断→行动）

---

### 6.9 观察空间总结

| 特性 | 值 |
|------|-----|
| **单智能体观察维度** | 23维 |
| **全局状态维度** | 23维 |
| **数值范围** | [-1, 1] (归一化后) |
| **信息不对称** | 投篮点位置（进攻方可见，防守方不可见） |
| **归一化方式** | 位置/场地尺寸，速度/v_max，时间/t_limit |
| **相对坐标** | 单智能体观察使用相对坐标 |
| **绝对坐标** | 全局状态使用绝对坐标 |
| **观测顺序** | 全局状态固定顺序，单智能体相对顺序 |
| **用途** | 观察→Actor，全局状态→Critic |

---

---

## 7. Attention 模型配置与观测对应关系

本节详细说明**观察空间**如何映射到**Attention模型配置**，重点解释Actor和Critic的不同输入结构。

---

### 7.1 配置文件概览

| 配置文件 | 用途 | 输入模式 | 关键特性 |
|---------|------|---------|---------|
| `attention_attacker.yaml` | 进攻方Actor | Ego Embedding | `use_ego_embedding: True` |
| `attention_defender.yaml` | 防守方Actor | Ego Embedding + 信息隐藏 | `ignore_features: [spot_embed]` |
| `attention_critic.yaml` | Centralized Critic | Global Embedding | `use_ego_embedding: False`, 包含全局时间 |

---

### 7.2 Actor 配置详解

#### 7.2.1 进攻方 Actor (`attention_attacker.yaml`)

**输入特征顺序**（必须严格按照此顺序）：

```yaml
input_feature_order:
  - self_embed        # Index 0 (7维) ← Ego Token
  - teammate_embed    # Index 1 (4维)
  - opponent_embed    # Index 2, 3 (4维, 共2个)
  - spot_embed        # Index 4 (2维) ← 进攻方可见
  - basket_embed      # Index 5 (2维)
```

**特征维度定义**：

| 特征名 | 维度 | 数量 | 内容 | 对应观察向量索引 |
|--------|------|------|------|----------------|
| `self_embed` | 7 | 1 | 自身位置(2) + 速度(2) + A1状态(1) + A1读条(1) + 时间(1) | [0:7] |
| `teammate_embed` | 4 | 1 | 队友相对位置(2) + 相对速度(2) | [7:11] |
| `opponent_embed` | 4 | 2 | 对手相对位置(2) + 相对速度(2) × 2 | [11:19] |
| `spot_embed` | 2 | 1 | 投篮点相对位置 | [19:21] |
| `basket_embed` | 2 | 1 | 篮筐相对位置 | [21:23] |

**关键配置**：
```yaml
use_ego_embedding: True  # 只取 Index 0 (self_embed) 做决策
num_attention_layers: 1  # 单层注意力机制
```

---

#### 7.2.2 防守方 Actor (`attention_defender.yaml`)

**输入特征顺序**（必须严格按照此顺序）：

```yaml
input_feature_order:
  - self_embed        # Index 0 (7维) ← Ego Token
  - teammate_embed    # Index 1 (4维)
  - opponent1_embed   # Index 2 (4维)
  - opponent2_embed   # Index 3 (4维)
  - spot_embed        # Index 4 (2维) ← 被忽略！
  - basket_embed      # Index 5 (2维)
```

**特征维度定义**：

| 特征名 | 维度 | 数量 | 内容 | 对应观察向量索引 |
|--------|------|------|------|----------------|
| `self_embed` | 7 | 1 | 自身位置(2) + 速度(2) + A1状态(1) + A1读条(1) + 时间(1) | [0:7] |
| `teammate_embed` | 4 | 1 | 队友相对位置(2) + 相对速度(2) | [7:11] |
| `opponent1_embed` | 4 | 1 | 对手1(A1)相对位置(2) + 相对速度(2) | [11:15] |
| `opponent2_embed` | 4 | 1 | 对手2(A2)相对位置(2) + 相对速度(2) | [15:19] |
| `spot_embed` | 2 | 1 | 投篮点相对位置（**被忽略**） | [19:21] → **丢弃** |
| `basket_embed` | 2 | 1 | 篮筐相对位置 | [21:23] |

**🔥 关键差异：信息隐藏**：

```yaml
ignore_features:
  - spot_embed  # 防守方看不到投篮点位置！
```

**设计意图**：
- 防守方通过 `ignore_features` 主动丢弃 `spot_embed` 特征
- 即使环境提供了投篮点位置（全0向量），模型层面确保不使用该信息
- 双重保障：环境返回全0 + 模型主动忽略

---

### 7.3 Critic 配置详解 (`attention_critic.yaml`)

#### 7.3.1 全局状态输入顺序

**输入特征顺序**（固定顺序，所有智能体共享）：

```yaml
input_feature_order:
  - attacker1_embed   # Index 0 (6维) ← A1状态
  - attacker2_embed   # Index 1 (4维) ← A2状态
  - defender_embed    # Index 2, 3 (4维, 共2个) ← D1, D2状态
  - spot_embed        # Index 4 (2维)
  - basket_embed      # Index 5 (2维)
  - time              # Global (1维) ← 全局时间
```

**特征维度定义**：

| 特征名 | 维度 | 数量 | 内容 | 对应全局状态索引 |
|--------|------|------|------|----------------|
| `attacker1_embed` | 6 | 1 | A1位置(2) + 速度(2) + 在圈内标志(1) + 读条进度(1) | [0:6] |
| `attacker2_embed` | 4 | 1 | A2位置(2) + 速度(2) | [6:10] |
| `defender_embed` | 4 | 2 | D1位置(2) + 速度(2), D2位置(2) + 速度(2) | [10:18] |
| `spot_embed` | 2 | 1 | 投篮点位置 | [18:20] |
| `basket_embed` | 2 | 1 | 篮筐位置 | [20:22] |
| `time` | 1 | 1 | 剩余时间比例 | [22:23] |

**关键配置**：
```yaml
use_ego_embedding: False  # 聚合所有Token，不使用Ego模式
num_attention_layers: 3   # 3层注意力机制（比Actor更深）
```

---

### 7.4 维度映射表：观察空间 → Attention 输入

#### 7.4.1 Actor 输入映射（以A1为例）

**观察向量 (23维)** → **Attention输入特征**

```
观察向量                    Attention输入特征
[0:2]   自身位置     →   self_embed[0:2]
[2:4]   自身速度     →   self_embed[2:4]
[4:5]   A1在圈内        →   self_embed[4:5]
[5:6]   A1读条进度         →   self_embed[5:6]
[6:7]   剩余时间             →   self_embed[6:7]
[7:9]   队友位置        →   teammate_embed[0:2]
[9:11]  队友速度        →   teammate_embed[2:4]
[11:13] 对手1位置(D1)     →   opponent_embed[0:2] (第1个)
[13:15] 对手1速度(D1)     →   opponent_embed[2:4] (第1个)
[15:17] 对手2位置(D2)     →   opponent_embed[0:2] (第2个)
[17:19] 对手2速度(D2)     →   opponent_embed[2:4] (第2个)
[19:21] 投篮点位置         →   spot_embed
[21:23] 篮筐位置           →   basket_embed
```

**关键差异**：
- **self_embed = 7维**：包含自身位置、速度、A1状态标志、A1读条进度、剩余时间
- **teammate/opponent_embed = 4维**：只包含相对位置和相对速度

---

#### 7.4.2 Critic 输入映射（全局状态）

**全局状态向量 (23维)** → **Attention输入特征**

```
全局状态                  Attention输入特征
[0:2]   A1位置        →   attacker1_embed[0:2]
[2:4]   A1速度        →   attacker1_embed[2:4]
[4:5]   A1在圈内          →   attacker1_embed[4:5]
[5:6]   A1读条进度         →   attacker1_embed[5:6]
[6:8]   A2位置        →   attacker2_embed
[8:10]  A2速度        →   attacker2_embed
[10:12] D1位置        →   defender_embed (第1个)
[12:14] D1速度        →   defender_embed (第1个)
[14:16] D2位置        →   defender_embed (第2个)
[16:18] D2速度        →   defender_embed (第2个)
[18:20] 投篮点位置         →   spot_embed
[20:22] 篮筐位置           →   basket_embed
[22:23] 剩余时间             →   time (全局特征)
```

**关键差异**：
- **attacker1_embed = 6维**：包含A1的位置、速度、在圈内标志、读条进度（不包含时间）
- **attacker2_embed = 4维**：只包含A2的位置和速度
- **defender_embed = 4维**：只包含防守方的位置和速度
- **time作为全局特征**：不进入Transformer，直接拼接到最后MLP

---

### 7.5 配置差异对比表

| 特性 | Attacker Actor | Defender Actor | Critic |
|------|---------------|---------------|--------|
| **use_ego_embedding** | True | True | False |
| **输入模式** | Ego Token | Ego Token | 全局聚合 |
| **输入维度** | 23维 | 23维 | 23维 |
| **spot_embed** | ✅ 可见 | ❌ 忽略 | ✅ 可见 |
| **time特征** | 在self_embed中 | 在self_embed中 | 单独全局特征 |
| **num_attention_layers** | 1 | 1 | 3 |
| **self_embed维度** | 7 | 7 | N/A (用attacker1_embed) |
| **对手表示** | opponent_embed (统一) | opponent1_embed + opponent2_embed (分开) | defender_embed (统一) |
| **参数共享** | 支持独立参数 | 支持独立参数 | 所有智能体共享 |

---

### 7.6 Attention 模型架构

#### 7.6.1 Actor 架构（Ego Embedding模式）

```
输入序列: [self_embed, teammate_embed, opponent_embed×2, spot_embed, basket_embed]
         [7维, 4维, 4维×2, 2维, 2维]

↓ Embedding Layer (独立编码器或共享编码器)

Token序列: [Token_0, Token_1, Token_2, Token_3, Token_4, Token_5]
           每个Token → 128维

↓ Multi-Head Self-Attention (1层)
  - Token之间的交互关系
  - Query/Key/Value机制

↓ 取出 Ego Token (Index 0)

Ego Token: [128维]

↓ Final MLP [256, 256]

输出: Action (连续或离散)
```

**Ego Embedding的意义**：
- 只使用 `self_embed` 对应的Token做决策
- 通过Attention机制，Ego Token已经聚合了与其他所有Token的关系
- 符合"以自我为中心"的分散式决策

---

#### 7.6.2 Critic 架构（全局聚合模式）

```
输入序列: [attacker1_embed, attacker2_embed, defender_embed×2, spot_embed, basket_embed]
         [6维, 4维, 4维×2, 2维, 2维]

全局特征: time [1维]

↓ Embedding Layer

Token序列: [Token_0, Token_1, Token_2, Token_3, Token_4, Token_5]
           每个Token → 128维

↓ Multi-Head Self-Attention (3层)
  - 深层交互关系
  - 全局态势理解

↓ 聚合策略 (Flatten或Mean)

全局表示: [Token_0, Token_1, ..., Token_5] → [128×6] 或 Mean → [128]

↓ 拼接全局特征: time

综合表示: [128×6+1] 或 [128+1]

↓ Final MLP [256, 256]

输出: Value Function (标量)
```

**不使用Ego Embedding的意义**：
- Critic需要评估全局状态，不能只看单个智能体
- 通过聚合所有Token，获得全局态势的完整表示
- 3层Attention提供更强的关系建模能力

---

### 7.7 配置验证清单

#### 7.7.1 Actor 配置验证

**检查项**：
1. ✅ `use_ego_embedding: True`（Actor必须为True）
2. ✅ `input_feature_order` 中 `self_embed` 必须排在第一位
3. ✅ `self_embed` 维度为7（位置2 + 速度2 + A1状态1 + A1读条1 + 时间1）
4. ✅ `teammate_embed` 和 `opponent_embed` 维度为4（位置2 + 速度2）
5. ✅ Defender配置中 `ignore_features` 包含 `spot_embed`

**常见错误**：
- ❌ `self_embed` 维度设为4（忘记包含A1状态和时间）
- ❌ `input_feature_order` 顺序错误（导致Ego Token取错）
- ❌ Defender忘记添加 `ignore_features`（防守方会看到投篮点）

---

#### 7.7.2 Critic 配置验证

**检查项**：
1. ✅ `use_ego_embedding: False`（Critic必须为False）
2. ✅ `input_feature_order` 使用固定智能体顺序（attacker1 → attacker2 → defender）
3. ✅ `attacker1_embed` 维度为6（位置2 + 速度2 + 在圈内1 + 读条进度1）
4. ✅ `attacker2_embed` 和 `defender_embed` 维度为4（位置2 + 速度2）
5. ✅ `time` 在 `roles.global` 中（作为全局特征）
6. ✅ `num_attention_layers: 3`（Critic使用更深的网络）

**常见错误**：
- ❌ `attacker1_embed` 维度设为4（忘记包含A1状态标志）
- ❌ `time` 放在 `entity` 而非 `global` 中（会进入Transformer）
- ❌ `use_ego_embedding: True`（Critic会只看第一个Token）

---

### 7.8 代码实现：特征提取

#### 7.8.1 Actor 特征提取逻辑

```python
def _get_actor_input_features(self, obs: TensorDict, agent_idx: int):
    """
    从观察向量中提取Actor需要的特征
    
    Args:
        obs: 观察向量 [batch, 23]
        agent_idx: 智能体索引 (0=A1, 1=A2, 2=D1, 3=D2)
    
    Returns:
        features: Dict[str, Tensor] 每个特征的张量
    """
    # 1. 提取自身特征 (7维)
    self_embed = torch.cat([
        obs[:, 0:2],   # 位置
        obs[:, 2:4],   # 速度
        obs[:, 4:5],   # A1在圈内
        obs[:, 5:6],   # A1读条进度
        obs[:, 6:7],   # 剩余时间
    ], dim=-1)  # [batch, 7]
    
    # 2. 提取队友特征 (4维)
    teammate_embed = torch.cat([
        obs[:, 7:9],   # 队友位置
        obs[:, 9:11],  # 队友速度
    ], dim=-1)  # [batch, 4]
    
    # 3. 提取对手特征 (4维 × 2)
    opponent_embed = torch.cat([
        obs[:, 11:15],  # 对手1位置+速度
        obs[:, 15:19],  # 对手2位置+速度
    ], dim=-1).view(batch_size, 2, 4)  # [batch, 2, 4]
    
    # 4. 提取投篮点和篮筐
    spot_embed = obs[:, 19:21]      # [batch, 2]
    basket_embed = obs[:, 21:23]    # [batch, 2]
    
    return {
        "self_embed": self_embed,
        "teammate_embed": teammate_embed,
        "opponent_embed": opponent_embed,
        "spot_embed": spot_embed,
        "basket_embed": basket_embed,
    }
```

---

#### 7.8.2 Critic 特征提取逻辑

```python
def _get_critic_input_features(self, global_state: TensorDict):
    """
    从全局状态中提取Critic需要的特征
    
    Args:
        global_state: 全局状态 [batch, 23]
    
    Returns:
        features: Dict[str, Tensor] 每个特征的张量
    """
    # 1. 提取A1特征 (6维)
    attacker1_embed = torch.cat([
        global_state[:, 0:2],   # A1位置
        global_state[:, 2:4],   # A1速度
        global_state[:, 4:5],   # A1在圈内
        global_state[:, 5:6],   # A1读条进度
    ], dim=-1)  # [batch, 6]
    
    # 2. 提取A2特征 (4维)
    attacker2_embed = torch.cat([
        global_state[:, 6:8],   # A2位置
        global_state[:, 8:10],  # A2速度
    ], dim=-1)  # [batch, 4]
    
    # 3. 提取防守方特征 (4维 × 2)
    defender_embed = torch.cat([
        global_state[:, 10:14],  # D1位置+速度
        global_state[:, 14:18],  # D2位置+速度
    ], dim=-1).view(batch_size, 2, 4)  # [batch, 2, 4]
    
    # 4. 提取环境特征
    spot_embed = global_state[:, 18:20]    # [batch, 2]
    basket_embed = global_state[:, 20:22]  # [batch, 2]
    time = global_state[:, 22:23]          # [batch, 1]
    
    return {
        "attacker1_embed": attacker1_embed,
        "attacker2_embed": attacker2_embed,
        "defender_embed": defender_embed,
        "spot_embed": spot_embed,
        "basket_embed": basket_embed,
        "time": time,
    }
```

---

### 7.9 配置调优建议

#### 7.9.1 Actor 配置调优

| 参数 | 当前值 | 调优建议 |
|------|--------|---------|
| `num_attention_layers` | 1 | 增加到2-3层可以建模更复杂的关系，但会增加计算量 |
| `num_heads` | 4 | 增加到8可以捕获更多样化的关系模式 |
| `embedding_dim` | 128 | 增加到256可以提升表达能力，但需要更多数据 |
| `final_mlp_hidden_layers` | [256, 256] | 可以尝试[128, 128]减少过拟合，或[512, 256]增加容量 |

---

#### 7.9.2 Critic 配置调优

| 参数 | 当前值 | 调优建议 |
|------|--------|---------|
| `num_attention_layers` | 3 | Critic应该比Actor更深，因为需要理解全局态势 |
| `use_ego_embedding` | False | **必须保持False**，Critic需要全局信息 |
| `encoder_groups` | 未启用 | 可以尝试将相同维度的特征分组共享编码器 |

---

### 7.10 总结：配置与观测的对应关系

| 观测内容 | Actor输入 | Critic输入 | 维度差异说明 |
|---------|----------|-----------|------------|
| **自身/A1状态** | `self_embed` [7维] | `attacker1_embed` [6维] | Actor包含时间，Critic单独处理时间 |
| **A2状态** | `teammate_embed` [4维] (A1视角) | `attacker2_embed` [4维] | 维度一致 |
| **防守方状态** | `opponent_embed` [4维×2] | `defender_embed` [4维×2] | 维度一致 |
| **投篮点位置** | `spot_embed` [2维] (进攻方) | `spot_embed` [2维] | Critic始终可见，防守方Actor被忽略 |
| **篮筐位置** | `basket_embed` [2维] | `basket_embed` [2维] | 维度一致 |
| **时间** | 在`self_embed`中 [1维] | 单独`time` [1维] | Actor嵌入自身，Critic作为全局特征 |

---

**文档版本**: 1.3  
**最后更新**: 2026-03-06